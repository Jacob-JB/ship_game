[package]
name = "client"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy.workspace = true
avian3d.workspace = true

nevy.workspace = true
rustls = { version = "0.23.5", default-features = false, features = ["std"] }
serde.workspace = true

common.path = "../common"
